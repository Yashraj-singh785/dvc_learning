1. Create git repository  dvc_learning and cloning it in local  .
2.Create a code1.py file -> make a csv file  to a data folder 
3.doing a git add + commit before initializing dvc .
4.## pip install dvc .
5.now we do initialization of the dvc throw "dvc init" -> creates a .dvcignore , .dvc
6.making a folder S3 "mkdir S3" (currently using the S3 for storing in upcomig we use aws )
7.now we do "dvc remote add -d myremote S3" -> telling dvc that S3 is our remote branch save change here.
8. next "dvc add data\" -> dvc responsible for any changes in data folder. ->data.dvc is created -> gives you id 
9. git commit -push  and dcv commit and then dvc push -> before doing see that s3 is empty 
10. git commit -push to mark this stage first version of data saved into s3 .
11. add changes in data in code1.py file (append a row)
 and chech changes (dvc status)
12. now agaian "dvc commit" and then "dvc push" -> see now the id is changed 
13. again git -commit -push "second version of the data "
14. now add one more data entry  then perform dvc -cmmit push and git push
15. git log --online (see) 
"git checkout<hash> - git checkout master" -> now dvc pull


